name: Claude Code Agent

on:
  pull_request:
    types: [opened, synchronize]
  issue_comment:
    types: [created]

permissions:
  id-token: write      # Required for secure OIDC token exchange
  contents: write
  pull-requests: write
  issues: write

jobs:
  claude_run:
    if: |
      (github.event_name == 'pull_request' && github.actor == 'codebreaker32') || 
      (github.event_name == 'issue_comment' && github.actor == 'codebreaker32' && contains(github.event.comment.body, '@claude'))
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run Claude Code Action
        uses: anthropics/claude-code-action@v1
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          allowed_tools: |
            Bash(python3 -m unittest discover tests -v)
            Bash(python3 -m pytest:*)
            Bash(gh issue create:*)
            Bash(gh issue edit:*)
            Bash(gh issue list:*)
            Bash(gh issue view:*)
            Bash(gh pr comment:*)
            Bash(gh pr view:*)